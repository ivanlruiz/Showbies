# Copiar a ShowBies1/keystore.local (gitignoreado) y completar. Lo lee
# Build > Android AAB (release). Formato clave=valor, sin comillas.
keystore=../release.keystore
alias=showbies
storepass=
keypass=
